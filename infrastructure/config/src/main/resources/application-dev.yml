# ============================================================
# CoAgent4U — Development Profile
# ============================================================

spring:
  datasource:
    url: jdbc:postgresql://ep-crimson-credit-airhrjj7-pooler.c-4.us-east-1.aws.neon.tech/CoAgent4U?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_1NhOTH5poMEk
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  flyway:
    clean-disabled: false

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.coagent4u: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
